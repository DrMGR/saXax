/** @type {import('next').NextConfig} */
const nextConfig = {}


module.exports = {
    nextConfig,
    // Other config options...
    output: {
        // Generate static HTML files for each route
        export: true,
    },
};
